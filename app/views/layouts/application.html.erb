<!DOCTYPE html>
<html>
  <head>
    <title>TwitchTvRandomStreamFinder</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <% flash.each do |type, msg| %>
    <div>
      <%= msg %>
    </div>
  <% end %>
   

  <h1>Welcome to The Random Stream Finder</h1>

  <nav class="navbar navbar-expand-md">
    <button class="navbar-toggler navbar-dark" type="button" data-toggle="collapse" data-target="#main-navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="main-navigation">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Home", root_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Recommended Streamers", recommendations_path, class: "nav-link" %>
        </li>
        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to "View Profile Page", user_path(current_user), class: 'nav-link' %>
          </li>
          <li class="nav-item">
              <%= link_to "logout", logout_path, class: "nav-link" %>
          </li>
        <% end %>

      </ul>
    </div>
  </nav>

<%= yield %>
</body>

</html>
